{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "check:unlock-worker": "node --check ./src/unlock-mi-easier.optimized.js",
    "fastboot:bridge": "node ./src/fastboot-bridge.mjs",
    "auth:session": "node ./src/xiaomi-session.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
